<link href="{{ bootstrap.css.url }}" rel="stylesheet"
      integrity="{{ bootstrap.css.integrity }}"
      crossorigin="anonymous">
<link href="{{ bootstrap.theme.url }}" rel="stylesheet"
      integrity="{{ bootstrap.theme.integrity }}"
      crossorigin="anonymous">
<link href="{{ bootstrap.ie10.css.url}}" rel="stylesheet"
      integrity="{{ bootstrap.ie10.css.integrity }}"
      crossorigin="anonymous">
